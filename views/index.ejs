<%- include('./partials/header') %>

    <div>
        <h1>Hello World!</h1>
    </div>

    <div>
        <button>New</button>
    </div>

    <div>
        <% subjuntivo.map(article => { %>
            <p><%= article.title %></p>
            <a href="<%= article.slug %>" >Read</a>
            <a href="/admin/edit/<%= article._id %>">Edit</a>
        <% }) %>
    </div>

<%- include('./partials/footer') %>